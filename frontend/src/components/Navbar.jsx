import React from 'react';
import '../App.css';

const Navbar = () => (
  <nav className="navbar">
    <span className="navbar-title">AutoStock.AI</span>
    <span className="navbar-user">Welcome, Admin</span>
  </nav>
);

export default Navbar;